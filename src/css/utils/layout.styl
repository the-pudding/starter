// -------------------
// Layouts
// -------------------

// Setting root sizes and base styles.

body
	font-feature-settings 'kern' 1, 'onum' 1, 'liga' 0, 'tnum' 1
	-webkit-font-smoothing antialiased
	-moz-osx-font-smoothing grayscale
	-webkit-tap-highlight-color rgba(0,0,0,0)
	background-color #fff
	color $off-black
	font-size set-type-size(root, false)
	+mq('bp-2')
		font-size set-type-size(medium, false)

h1
	set-type-size(xxx-large, true)
	margin 1.5rem 0
	font-weight bold
h2
	set-type-size(xx-large, true)
	margin 1.5rem 0
	font-weight bold
h3
	set-type-size(x-large, true)
	margin 1.5rem 0
	font-weight bold
h4
	set-type-size(large, true)
	margin 1.5rem 0
	font-weight bold
h5
	set-type-size(medium, true)
	margin 1.5rem 0
	font-weight bold
h6
	set-type-size(root, true)
	margin 1.5rem 0
	font-weight bold
p 
	margin 1.5rem 0

a
	text-decoration none
	border-bottom 1px solid $primary
	color $off-black

	&:visited, &:hover
		color $off-black

strong, b
	font-weight bold

em, i
	font-style italic
	
ul
	list-style-type none

li
	set-type-size(medium, true)

img, video
	display block
	width 100%

button
	cursor pointer
	border none
	outline none
	margin 0
	padding 0
	font-size 1rem

sub, sup
	font-size 0.75rem
	line-height 0
	position relative
	vertical-align baseline

sup
	top -0.5em

sub
	bottom -0.25em
